node_modules/
.pnpm-store/
dist/
.next/
out/

# Never commit credentials. Signer secrets belong only in process-specific files.
.env
.env.agent
.env.executor
.env.authorizer
.env.reviewer
.env.reconciler
.env.anchor
.env.local
.env.*.local

*.log
npm-debug.log*
pnpm-debug.log*

# Release evidence must survive a clean clone even though ordinary logs stay local.
!artifacts/final-evidence/terminal/*.log
!artifacts/judge-evidence/**/*.log

.DS_Store
.idea/
.vscode/
.claude/

coverage/
*.tsbuildinfo

# Local Postgres volume if bind-mounted
pgdata/

# Local crash dumps and quarantined build artifacts
/core
/core.*
.local-trash/

# Local review notes and generated submission/deck artifacts
ReviewByHarsh/
output/
tmp/

# Abandoned/local video-editing assets and narration assembly files
docs/assets/video/
