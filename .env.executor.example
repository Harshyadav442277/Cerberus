# Copy to .env.executor. Load only in the isolated executor or standalone x402
# diagnostics. Never expose this variable to apps/agent, apps/api, or apps/merchant.
EXECUTOR_DATABASE_URL=postgres://cerberus_executor_app:CHANGE_ME_16_CHARS_MIN@localhost:5544/safr_runtime
EXECUTOR_EVM_PRIVATE_KEY=
