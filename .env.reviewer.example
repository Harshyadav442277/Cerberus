# Copy to .env.reviewer. Only the trusted scripted-reviewer process reads this file.
# Use the same high-entropy token configured for the API and dashboard server.

CERBERUS_API_URL=http://localhost:4050
REVIEWER_API_TOKEN=
